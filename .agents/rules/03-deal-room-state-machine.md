# 03-deal-room-state-machine

- Deal Room должен иметь строго определённые состояния.
- Переходы между состояниями должны быть документированы и валидированы.
- События всегда логируются, а аудита хватает для восстановления истории.
- Статус нельзя менять произвольно без подтверждённого события.
